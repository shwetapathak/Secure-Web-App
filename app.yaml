application: secureassign2
runtime: php55
api_version: 1

env_variables:
  # Replace project, instance, database, user and password with the values obtained
  # when configuring your Cloud SQL instance.
  MYSQL_DSN: mysql:unix_socket=/cloudsql/secureassign2:us-central1:myinstance;dbname=users
  MYSQL_USER: root
  MYSQL_PASSWORD: 'shweta2191'
handlers:
- url: /.*
  script: main.php
- url: /
  script: main.php
 
- url: /login.php
  script: login.php
